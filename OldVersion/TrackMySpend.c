#include "module4.h"  // Include the header that defines the `app()` function and other required dependencies

// Entry point of the program
int main()
{
    app();            // Call the main application function to launch the expense tracker system
    return 0;         // Return 0 to indicate successful execution
}
